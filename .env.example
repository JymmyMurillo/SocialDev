# =====================================================
# Variables de Entorno - SocialDev
# =====================================================
# Copia este archivo como .env y ajusta los valores según necesites
# Comando: cp .env.example .env

# =====================================================
# Database Configuration (PostgreSQL)
# =====================================================
DB_USER=socialdev
DB_PASSWORD=socialdev123
DB_NAME=socialdev_db
DB_HOST=localhost
DB_PORT=5432

# URL de conexión completa (usada por Prisma)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL=postgresql://socialdev:socialdev123@localhost:5432/socialdev_db?schema=public

# =====================================================
# Backend Configuration
# =====================================================
# Puerto donde corre el servidor NestJS
PORT=3000

# Entorno de ejecución (development | production | test)
NODE_ENV=development

# Secreto para firmar tokens JWT (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET=super-secret-key-change-in-production-please

# Expiración del token JWT (ejemplos: 1h, 24h, 7d, 30d)
JWT_EXPIRES_IN=24h

# =====================================================
# Frontend Configuration
# =====================================================
# URL de la API backend (sin slash al final)
VITE_API_URL=http://localhost:3000

# =====================================================
# CORS Configuration
# =====================================================
# URLs permitidas para CORS (separadas por coma)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# =====================================================
# Docker Configuration
# =====================================================
# Estos valores son usados por docker-compose.yml
# Si cambias algo aquí, recuerda reconstruir los contenedores:
# docker-compose up -d --build

# =====================================================
# Valores para Desarrollo Local (SIN Docker)
# =====================================================
# Si ejecutas el backend localmente sin Docker, usa:
# DATABASE_URL=postgresql://socialdev:socialdev123@localhost:5432/socialdev_db?schema=public

# =====================================================
# Valores para Docker Compose
# =====================================================
# Si usas docker-compose, el backend debe conectarse así:
# DATABASE_URL=postgresql://socialdev:socialdev123@db:5432/socialdev_db?schema=public
# (Nota: "db" es el nombre del servicio en docker-compose.yml)
